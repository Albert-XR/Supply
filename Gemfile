source 'https://rubygems.org'

git_source(:github) {|supply| "https://github.com/YJPL/{supply}" }

gem "jekyll"

# 锁定 sass-embedded 版本，避免构建失败
gem "sass-embedded", "~> 1.69.0"

group :jekyll_plugins do
  gem "jekyll-leaflet"
  gem 'jekyll-seo-tag'
  gem 'jekyll-sitemap'
  gem 'jekyll-feed'
end
